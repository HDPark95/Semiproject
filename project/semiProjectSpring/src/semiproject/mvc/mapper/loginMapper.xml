<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="login">
	<select id="login" parameterType="String" resultType="uservo"> select * from signup s, member_detail d where aid = #{s.aid} and d.anum = s.anum </select>
</mapper> 