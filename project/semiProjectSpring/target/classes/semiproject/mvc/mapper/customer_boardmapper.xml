<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="board">
<insert id="add" parameterType="cbvo"> <!-- 게시글 작성 -->
insert into customer_board(c_num,c_type,c_writer,c_subject,c_content,c_pwd) 
values(customer_board_seq.nextVal,#{c_type},#{c_writer},#{c_subject},#{c_content},#{c_pwd})
</insert>

<select id="list" resultType="cbvo">
select c_num,c_subject,c_writer,c_regdate,c_anschk from customer_board order by c_num desc 
</select> 


<select id="viewDetail" resultType="cbvo" parameterType="int">
select c_type,c_writer,c_subject,c_content,c_ans from customer_board where c_num = #{c_num}
</select>

<update id="ans" parameterType="cbvo">
update customer_board set c_ans = #{c_ans} where c_num=#{c_num};
</update> 

</mapper>  